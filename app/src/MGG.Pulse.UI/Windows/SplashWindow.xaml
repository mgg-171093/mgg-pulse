<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MGG.Pulse.UI.Windows.SplashWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Width="600" Height="750" Background="{ThemeResource BackgroundBrush}">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="28">
            <Image
                x:Name="LogoImage"
                Width="400"
                Height="450"
                HorizontalAlignment="Center"
                Stretch="Uniform" />

            <ProgressBar
                x:Name="InitProgress"
                Width="260"
                Height="4"
                Minimum="0"
                Maximum="100"
                Value="0"
                Background="{ThemeResource BorderBrush}"
                Foreground="{ThemeResource PrimaryBrush}" />
        </StackPanel>
    </Grid>
</Window>
