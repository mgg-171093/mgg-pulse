{
  "_comment": [
    "This file is committed on main and served from the repo raw URL as 'latest.json'.",
    "The auto-updater in MGG Pulse fetches it at startup and every 4 hours.",
    "",
    "REQUIRED fields:",
    "  version  — semantic version string (major.minor.patch), e.g. '1.3.1'",
    "  url      — direct download URL of the installer .exe",
    "  sha256   — lowercase hex SHA-256 digest of the installer .exe",
    "",
    "OPTIONAL fields:",
    "  notes    — short release notes shown to the user before updating"
  ],
  "version": "1.3.1",
  "url": "https://github.com/mgg-171093/mgg-pulse/releases/download/v1.3.1/MGGPulse-Setup-1.3.1.exe",
  "sha256": "abba80b86f6180acc70c3ae8e693765429f753850e8b134cec51b6885ab91d00",
  "notes": "Patch release: updater manifest resolution now follows the raw main branch model for reliable latest.json checks."
}
